// #1
const legalAge = 18;
const exampleAge = 34;
const canDrive = (exampleAge >= legalAge) ? "Yes, you can drive" : "Nope, you're too young";
console.log(canDrive);

// #2
const legalSpeed = 130;
const exampleSpeed = 120;
const speedCheck = (exampleSpeed > legalSpeed) ? "You're going too fast - slow down!" : "You're driving below the speed limit.";
console.log(speedCheck);